# Node dependencies and build outputs
node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment files - DO NOT COMMIT 🔒
# These files may contain secrets (API keys, database URLs, or private keys).
# Keep them out of version control and add a sample `.env.example` instead.
.env
.env.local
.env.*

# Allow the example env file (safe to commit)
!.env.example