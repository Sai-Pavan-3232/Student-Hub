# Copy to .env and fill in values for local development
# Keep .env out of version control; commit only this example file.

DATABASE_URL=https://obmzxsxxhjhrnqwymsmd.supabase.co
SESSION_SECRET=<REDACTED - ROTATE THIS SECRET>
PORT=5000

# Supabase client configuration (public)
# Copy the VITE_* keys into your `client/.env.local` so the browser can use the public anon key.
VITE_SUPABASE_URL=https://xyzcompany.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.example

# Supabase server configuration (PRIVATE - put in your local `.env` only)
# These keys are sensitive and should never be committed. The Admin/Service Role key must only be used on the server.
SUPABASE_URL=https://xyzcompany.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<REDACTED - service role key goes here>

# Enable server-side admin-only endpoints for debugging (set to true to expose admin routes)
# Do NOT enable in production unless you know what you're doing
ENABLE_ADMIN_ROUTES=false

# Notes:
# - VITE_ prefix is required for Vite to expose variables to the client
# - Do NOT commit your real .env or client/.env.local to source control; use `.env.example` instead
# - The server `SUPABASE_SERVICE_ROLE_KEY` allows admin operations and must remain secret

