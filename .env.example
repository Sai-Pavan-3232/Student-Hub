# Copy to .env and fill in values for local development
# Keep .env out of version control; commit only this example file.

DATABASE_URL=https://obmzxsxxhjhrnqwymsmd.supabase.co
SESSION_SECRET=<REDACTED - ROTATE THIS SECRET>
PORT=5000

# Supabase client configuration (public)
# Copy these into your `.env.local` and replace with your project's values from Supabase -> Settings -> API
VITE_SUPABASE_URL=https://xyzcompany.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.example

# Notes:
# - VITE_ prefix is required for Vite to expose variables to the client
# - Never commit your real .env.local to source control

