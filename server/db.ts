// Database disabled: exporting empty placeholders so the app runs with mock storage only.
export const pool = undefined;
export const db = undefined;

// If you later re-enable a real database, replace this file with a proper implementation.
